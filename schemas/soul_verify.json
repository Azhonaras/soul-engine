{
  "name": "soul_verify",
  "description": "Token-overlap NLI heuristic plus epistemic rank (not a transformer).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "episode_id": {
        "type": "string",
        "description": "Episode ID to verify"
      }
    },
    "required": [
      "episode_id"
    ]
  }
}
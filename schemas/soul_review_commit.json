{
  "name": "soul_review_commit",
  "description": "Enforce pre-commit checks 13-17 and atomically promote memories into active set with cryptographic receipt.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "cycle_id": {
        "type": "string"
      },
      "commit_human_event_ref": {
        "type": "string",
        "description": "Human host event ID approving the preview hash"
      }
    },
    "required": [
      "cycle_id",
      "commit_human_event_ref"
    ]
  }
}
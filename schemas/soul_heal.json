{
  "name": "soul_heal",
  "description": "Trigger automated state repair for the soul engine (level 1=routine recalibration, 2=rollback, 3=reset). Levels >= 2 are Tier 2 destructive operations requiring soul_host approval.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "level": {
        "type": "integer",
        "default": 1,
        "description": "Healing level (1=homeostasis recalibration, 2=rollback, 3=quarantine freeze)"
      },
      "session_id": {
        "type": "string",
        "description": "Chat session identifier for chat-authorized level 1 heal"
      },
      "reason": {
        "type": "string",
        "default": "Automated health repair"
      }
    }
  }
}
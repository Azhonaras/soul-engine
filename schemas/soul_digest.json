{
  "name": "soul_digest",
  "description": "Session start: traits, behavior orders, due flags, this-plan working traces, session overlay, and newest reviewed facts. Follow behavior. Unreviewed episodes omitted.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "integer",
        "default": 5
      },
      "plan_id": {
        "type": "string",
        "description": "Optional: this-plan working + overlay"
      },
      "agent_id": {
        "type": "string",
        "description": "Optional: this sub-agent overlay and traces"
      }
    }
  }
}
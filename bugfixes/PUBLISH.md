# 1.1.1 notes

Human origin: type **SEAL** in any MCP chat to approve quarantined memory. MCP cannot mint `origin_kind=human`. Then in a tty: `py -3 -m soul_host seal review_packet.json` (type **SEAL** then **COMMIT**).

NLI is a token heuristic. Default recall is reviewed memory after commit.

Tests: `python -m unittest discover -s tests`
